@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>

    <ul>
        <li>Singleton: @Model.SingletonService1.GetHashCode()</li>
        <li>Singleton: @Model.SingletonService2.GetHashCode()</li>
        <li>Scope: @Model.ScopeService1.GetHashCode()</li>
        <li>Scope: @Model.ScopeService2.GetHashCode()</li>
        <li>Transient: @Model.TransientService1.GetHashCode()</li>
        <li>Transient: @Model.TransientService2.GetHashCode()</li>
    </ul>
</div>
